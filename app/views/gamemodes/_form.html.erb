<br/>
<% if @gamemode.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@gamemode.errors.count, "error") %> prohibited this article from being saved:</h2>  
      <ul>
        <% @gamemode.errors.full_messages.each do |msg| %>
          <li><%= msg %></li>
        <% end %>
      </ul>
    </div>
<% end %>

<%= form_with model: @gamemode do |f| %>

    <%= f.label :mode_name%>
    <%= f.text_area :mode_name%> <br>

    <%= f.submit @gamemode.persisted? ? 'Save Changes' : 'Create game mode' %><br>
  
<% end %>
